<%= form_for(labor_process) do |f| %>
  <% if labor_process.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(labor_process.errors.count, "error") %> prohibited this labor_process from being saved:</h2>

      <ul>
      <% labor_process.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :process_category %>
    <%= f.number_field :process_category %>
  </div>

  <div class="field">
    <%= f.label :process_number %>
    <%= f.number_field :process_number %>
  </div>

  <div class="field">
    <%= f.label :distribution_date %>
    <%= f.date_select :distribution_date %>
  </div>

  <div class="field">
    <%= f.label :stick %>
    <%= f.text_field :stick %>
  </div>

  <div class="field">
    <%= f.label :court %>
    <%= f.text_field :court %>
  </div>

  <div class="field">
    <%= f.label :movements %>
    <%= f.text_area :movements %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.check_box :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
